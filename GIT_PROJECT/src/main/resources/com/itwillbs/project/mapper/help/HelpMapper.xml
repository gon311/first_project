<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.itwillbs.project.help.mapper.HelpMapper">

	<!-- ==================================================================== -->
	<!-- [공지사항 게시판] -->
	<!-- 공지사항 게시글 수 조회 -->
	<select id="getNoticeTotalCount" resultType="int">
		SELECT
			COUNT(*)
		FROM
			notice
		 <where>
		 	status = 'Y'
		 	<if test = 'keyword != null and keyword != "" '>
		 		<choose>
		 			<when test = 'type == "all"'>
		 				AND 
		 				(notice_title LIKE CONCAT ('%', #{keyword}, '%')
		 				OR status LIKE CONCAT ('%', #{keyword}, '%')
		 				OR user_type LIKE CONCAT ('%', #{keyword}, '%')
		 				)
		 			</when>
		 			<when test ='type == "title"'>
		 				AND notice_title LIKE CONCAT('%', #{keyword}, '%')
		 			</when>
		 			
		 			<when test = 'type == "status"'>
		 				AND status = #{keyword}
		 			</when>
		 			<when test = 'type == "userType"'>
		 				AND user_type = #{keyword}
		 			</when>
		 		</choose>
			</if>
		 </where>
	
	</select>
	
	<!-- 공지사항 리스트 조회 -->
	<select id="getNoticeList" resultType="com.itwillbs.project.admin.dto.NoticeDTO">
		SELECT
		 	notice_id
		 	, notice_title
		 	, notice_content
		 	, reg_date
		 	, readcount
		 	, user_type
		 	, update_date
		 	, status
		 FROM notice
		 <where>
		 	status = 'Y'
		 	<if test = 'keyword != null and keyword != "" '>
		 		<choose>
		 			<when test = 'type == "all"'>
		 				AND 
		 				(notice_title LIKE CONCAT ('%', #{keyword}, '%')
		 				OR status LIKE CONCAT ('%', #{keyword}, '%')
		 				OR user_type LIKE CONCAT ('%', #{keyword}, '%')
		 				)
		 			</when>
		 			<when test ='type == "noticeTitle"'>
		 				AND notice_title LIKE CONCAT('%', #{keyword}, '%')
		 			</when>
		 			
		 			<when test = 'type == "status"'>
		 				AND status =  LIKE CONCAT ('%', #{keyword}, '%')
		 			</when>
		 			<when test = 'type == "userType"'>
		 				AND user_type  LIKE CONCAT ('%', #{keyword}, '%')
		 			</when>
		 		</choose>
			</if>
		 </where>
		 LIMIT ${offset}, ${limit}
	</select>
	
	<!-- 공지사항 상세 조회 -->
	<select id="getNoticeById" resultType = "com.itwillbs.project.admin.dto.NoticeDTO">
		SELECT 
			notice_id
			, notice_title
			, notice_content
			, user_type
			, reg_date
			, update_date
			, readcount
			, status
		FROM notice
		WHERE notice_id = #{noticeId}
	
	</select>
	
	<!-- 조회수 증가 -->
	<update id="updateReadCount">
		UPDATE notice
		SET readcount = readcount + 1
		WHERE notice_id = #{noticeId}
	</update>
	
	<!--  ========================================================================= -->
	
	</mapper>