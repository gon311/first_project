<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itwillbs.project.board.mapper.BoardMapper">
	
	<!-- 게시글 등록 -->
	<insert id="insertBoard"
	        useGeneratedKeys="true"
	        keyProperty="postId">
	    INSERT INTO BOARD_POST (
	        board_type,
	        author_member_id,
	        title,
	        content,
	        status
	    ) VALUES (
	        #{boardType},
	        #{authorMemberId},
	        #{title},
	        #{content},
	        'ACTIVE'
	    )
	</insert>
	
	
	<!-- 게시물 상세 -->
	<select id="selectBoard" resultType="com.itwillbs.project.board.dto.BoardDTO">
	    SELECT
	        post_id AS postId,
	        board_type AS boardType,
	        CASE
	            WHEN board_type = 'JOB' THEN '취준/이직'
	            WHEN board_type = 'CAREER' THEN '회사생활/커리어'
	            WHEN board_type = 'FREE' THEN '자유주제'
	            WHEN board_type = 'INTERVIEW_REVIEW' THEN '면접후기'
	        END AS categoryName,
	        author_member_id AS authorMemberId,
	        title,
	        content,
	        readcount,
	        status,
	        created_at AS createdAt,
	        updated_at AS updatedAt
	    FROM BOARD_POST
	    WHERE post_id = #{postId}
	      AND status = 'ACTIVE'
	</select>
	
	<!-- 게시글 작성자 조회 -->
	<select id="selectAuthorMemberIdByPostId" resultType="long">
	    SELECT author_member_id
	    FROM BOARD_POST
	    WHERE post_id = #{postId}
	      AND status = 'ACTIVE'
	</select>

	<!-- 신고 누적 횟수 증가 -->
	<update id="increaseReportReceivedCount">
	    UPDATE USER_PERSON
	    SET report_received_count = report_received_count + 1
	    WHERE user_id = #{userId}
	</update>	
	
	
	<!-- 태그 저장 -->
	<insert id="insertBoardTag">
	    INSERT INTO BOARD_TAG (
	        post_id,
	        tag_name
	    ) VALUES (
	        #{postId},
	        #{tagName}
	    )
	</insert>
	
	<!-- 게시글 태그 조회 -->
	<select id="selectBoardTags" resultType="string">
	    SELECT tag_name
	    FROM BOARD_TAG
	    WHERE post_id = #{postId}
	    ORDER BY tag_id ASC
	</select>
	
	<!-- 태그 삭제 -->
	<delete id="deleteBoardTags">
	    DELETE FROM BOARD_TAG
	    WHERE post_id = #{postId}
	</delete>		
	
	<!-- 조회수 증가 -->
	<update id="updateReadcount">
		UPDATE BOARD_POST
		SET readcount = readcount + 1
		WHERE post_id = #{postId}
	</update>
	
	<!-- 게시물 수정 -->
	<update id="updateBoard">
		UPDATE BOARD_POST
		SET
		board_type = #{boardType},
		title = #{title},
		content = #{content},
		updated_at = NOW()
		WHERE post_id = #{postId}
	</update>
	
	<!-- 게시물 삭제 -->
	<update id="deleteBoard">
		UPDATE BOARD_POST
		SET status = 'DELETED'
		WHERE post_id = #{postId}
	</update>
	
	
	<!-- 파일 추가 -->
	<insert id="insertBoardFile">
	    INSERT INTO uploaded_file (
	        post_id,
	        category_idx,
	        origin_name,
	        stored_name,
	        file_path,
	        file_size,
	        file_ext
	    ) VALUES (
	        #{postId},
	        5,
	        #{fileDTO.originName},
	        #{fileDTO.storedName},
	        #{fileDTO.filePath},
	        #{fileDTO.fileSize},
	        #{fileDTO.fileExt}
	    )
	</insert>
	
	<!-- 게시글 목록 -->
	<select id="selectBoardList"
	        parameterType="com.itwillbs.project.board.dto.BoardCond"
	        resultType="com.itwillbs.project.board.dto.BoardDTO">
	
	SELECT
	    p.post_id AS postId,
	    p.board_type AS boardType,
	    CASE
	        WHEN p.board_type = 'JOB' THEN '취준/이직'
	        WHEN p.board_type = 'CAREER' THEN '회사생활/커리어'
	        WHEN p.board_type = 'FREE' THEN '자유주제'
	        WHEN p.board_type = 'INTERVIEW_REVIEW' THEN '면접후기'
	    END AS categoryName,
	    p.title,
	    p.content,
	    p.readcount AS viewCount,
	    p.created_at AS createdAt,
	    p.updated_at AS updatedAt,
	    u.user_name AS writerNickname,
	    LEFT(p.content, 120) AS excerpt,
	    (
	        SELECT COUNT(*)
	        FROM BOARD_COMMENT c
	        WHERE c.post_id = p.post_id
	          AND c.status = 'ACTIVE'
	    ) AS commentCount,
	    0 AS likeCount,
	    0 AS scrapCount
	FROM BOARD_POST p
	JOIN USER u
	  ON p.author_member_id = u.user_id
	WHERE p.status = 'ACTIVE'
	
	<if test="category != null and category != 'ALL'">
	    AND p.board_type = #{category}
	</if>
	
	<if test="q != null and q != ''">
	    <choose>
	        <when test="searchType == 'title'">
	            AND p.title LIKE CONCAT('%', #{q}, '%')
	        </when>
	        <when test="searchType == 'content'">
	            AND p.content LIKE CONCAT('%', #{q}, '%')
	        </when>
	        <otherwise>
	            AND (
	                p.title LIKE CONCAT('%', #{q}, '%')
	                OR p.content LIKE CONCAT('%', #{q}, '%')
	            )
	        </otherwise>
	    </choose>
	</if>
	
	ORDER BY
	<choose>
	    <when test="sort == 'view'">
	        p.readcount DESC
	    </when>
	    <when test="sort == 'comment'">
	        (
	            SELECT COUNT(*)
	            FROM BOARD_COMMENT c
	            WHERE c.post_id = p.post_id
	              AND c.status = 'ACTIVE'
	        ) DESC
	    </when>
	    <otherwise>
	        p.created_at DESC
	    </otherwise>
	</choose>
	
	LIMIT #{page.offset}, #{page.safeSize}
	
	</select>
	
	<!-- 게시글 총 갯수 -->
	<select id="selectBoardCount"
	        parameterType="com.itwillbs.project.board.dto.BoardCond"
	        resultType="int">
	
	SELECT COUNT(*)
	FROM BOARD_POST p
	
	WHERE p.status = 'ACTIVE'
	
	<if test="category != null and category != 'ALL'">
	    AND p.board_type = #{category}
	</if>
	
	<if test="q != null and q != ''">
	    AND (
	        p.title LIKE CONCAT('%', #{q}, '%')
	        OR p.content LIKE CONCAT('%', #{q}, '%')
	    )
	</if>
	
	</select>
	
	
	<!-- 파일 다운 -->
	<select id="selectBoardFiles" resultType="com.itwillbs.project.common.dto.FileDTO">
	    SELECT
	        file_id      AS fileId,
	        category_idx AS categoryIdx,
	        origin_name  AS originName,
	        stored_name  AS storedName,
	        file_path    AS filePath,
	        file_size    AS fileSize,
	        file_ext     AS fileExt,
	        reg_date     AS createdAt
	    FROM uploaded_file
	    WHERE post_id = #{postId}
	    ORDER BY file_id ASC
	</select>

	<select id="selectFileById" resultType="com.itwillbs.project.common.dto.FileDTO">
	    SELECT
	        file_id      AS fileId,
	        category_idx AS categoryIdx,
	        origin_name  AS originName,
	        stored_name  AS storedName,
	        file_path    AS filePath,
	        file_size    AS fileSize,
	        file_ext     AS fileExt,
	        reg_date     AS createdAt
	    FROM uploaded_file
	    WHERE file_id = #{fileId}
	</select>
		
	
	<!-- 파일삭제 -->
	<delete id="deleteBoardFile">
	    DELETE FROM uploaded_file
	    WHERE file_id = #{fileId}
	</delete>
	
	
</mapper>

