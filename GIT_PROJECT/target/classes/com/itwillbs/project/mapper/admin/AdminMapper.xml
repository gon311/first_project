<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.itwillbs.project.admin.mapper.AdminMapper">

	<select id="selectUser" resultType="member">
		select *
		from user
	</select>

	<!-- 구직자 전체 목록 조회(필터링) -->
	<select id="selectUserFilter" resultType="member">
		select 
			user_id
			,email
			,password
			,phone
			,user_name
			,user_type
			,status
			,joined_at
			,withdrawn_at
		from 
			user
		where
			user_type = #{user_type}
		and
			status = #{status}
		and
			user_name = #{user_name}
	</select>
	
	<!-- 구직자 상세정보 조회 -->
	<select id="selectUserInfo" resultType="member">
		select 
			user_id
			,email
			,password
			,phone
			,user_name
			,user_type
			,status
			,joined_at
			,withdrawn_at
		from 
			user
		where
			user_id = #{id}
		
	</select>

	<!-- 공지사항 리스트 조회 -->
	<select id="getNoticeList" resultType="notice">
		SELECT
		 	notice_id
		 	, notice_title
		 	, notice_content
		 	, reg_date
		 	, readcount
		 	, user_type
		 	, update_date
		 	, status
		 FROM notice
	</select>
	
	<insert id ="insertNotice" parameterType="notice">
		INSERT INTO notice (
			notice_title
			, notice_content
			, reg_date
			, readcount
			, user_type
			, status
		) VALUES (
			#{notice_title}
			, #{notice_content}
			, NOW()
			,0
			, #{user_type}
			, #{status}
		)
	</insert>
	
</mapper>