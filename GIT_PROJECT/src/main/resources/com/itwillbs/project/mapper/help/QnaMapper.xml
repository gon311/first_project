<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.itwillbs.project.help.mapper.QnaMapper">
	
    <insert id="insertQna" parameterType="com.itwillbs.project.help.dto.SupportQnaDTO" 
            useGeneratedKeys="true" keyProperty="qnaId">
        INSERT INTO qna (
            qna_category, qna_title, qna_content, writer_id
        ) VALUES (
            #{qnaCategory}, #{qnaTitle}, #{qnaContent}, #{writerId}
        )
    </insert>

    <insert id="insertFile" parameterType="com.itwillbs.project.common.dto.FileDTO">
        INSERT INTO uploaded_file (
            qna_id, category_idx, origin_name, stored_name, 
            file_path, file_size, file_ext
        ) VALUES (
            #{qnaId}, #{categoryIdx}, #{originName}, #{storedName}, 
            #{filePath}, #{fileSize}, #{fileExt}
        )
    </insert>
    
    <select id="selectQnaList" resultType="com.itwillbs.project.help.dto.SupportQnaDTO">
	    SELECT qna_id, qna_category, qna_title, reg_date, re_status
	    FROM qna
	    WHERE writer_id = #{writerId} AND is_deleted = 'N'
	    ORDER BY reg_date DESC
	</select>

	<select id="selectQnaDetail" parameterType="int" resultType="com.itwillbs.project.help.dto.SupportQnaDTO">
	    SELECT 
	        qna_id, 
	        qna_category, 
	        qna_title, 
	        qna_content, 
	        writer_id, 
	        reg_date, 
	        re_status, 
	        re_date, 
	        re_content, 
	        is_deleted
	    FROM qna
	    WHERE qna_id = #{qnaId} 
	      AND is_deleted = 'N'
	</select>
	
	<update id="deleteQna" parameterType="int">
	    UPDATE qna 
	    SET is_deleted = 'Y' 
	    WHERE qna_id = #{qnaId}
	</update>
	
	<select id="getFileList" resultType="fileDTO">
	    SELECT 
	        file_id AS fileId,
	        job_id AS idx,              
	        category_idx AS categoryIdx,
	        origin_name AS originName,
	        stored_name AS storedName,
	        file_path AS filePath,
	        file_size AS fileSize,
	        file_ext AS fileExt
	    FROM uploaded_file
	    WHERE qna_id = #{qnaId}
	</select>
	

</mapper>