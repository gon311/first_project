<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.itwillbs.project.admin.mapper.AdminMapper">

	<!-- 구직자 목록 조회 -->
	<resultMap id="memberResultMap" type="com.itwillbs.project.admin.dto.MemberDTO">
		<id property="id" column="user_id" />
		<result property="email" column="email" />
		<result property="password" column="password" />
		<result property="phone" column="phone" />
		<result property="name" column="user_name" />
		<result property="memberType" column="user_type" />
		<result property="status" column="status" />
		<result property="joinedAt" column="joined_at" />
		<result property="withdrawnAt" column="withdrawn_at" />
	</resultMap>
	
	<select id="selectUserList" resultMap="memberResultMap">
		select 
			*
		from 
			user
		where
			user_type = 'c'
			<if test="status != null and !status.equals('')">
				and
					status = #{status}
			</if>
			<if test="keyword != null and !keyword.equals('')">
				and
					user_name like concat('%',#{keyword},'%')
			</if>
	</select>
	
	<!-- 구직자 상세정보 조회 -->
	<select id="selectUserInfo" resultMap="memberResultMap">
		select 
			user_id
			, email
			, password
			, phone
			, user_name
			, user_type
			, status
			, joined_at
			, withdrawn_at
		from 
			user
		where
			user_id = #{id}
		
	</select>

	<!-- ==================================================================== -->
	<!-- 공지사항 리스트 조회 -->
	<select id="getNoticeList" resultType="notice">
		SELECT
		 	notice_id
		 	, notice_title
		 	, notice_content
		 	, reg_date
		 	, readcount
		 	, user_type
		 	, update_date
		 	, status
		 FROM notice
	</select>
	<!-- 공지사항 작성 -->
	<insert id ="insertNotice" parameterType="notice">
		INSERT INTO notice (
			notice_title
			, notice_content
			, reg_date
			, readcount
			, user_type
			, status
		) VALUES (
			#{notice_title}
			, #{notice_content}
			, NOW()
			,0
			, #{user_type}
			, #{status}
		)
	</insert>
	<!-- 공지사항 상세 조회 -->
	<select id="getNoticeById" resultType = "notice">
		SELECT 
			notice_title
			, notice_content
			, reg_date
			, readcount
		FROM notice
		WHERE notice_id = #{notice_id}
	
	</select>
	
	<!-- 조회수 증가 -->
	<update id="updateReadCount">
		UPDATE notice
		SET readcount = readcount + 1
		WHERE notice_id = #{notice_id}
	</update>
	
	<!-- ================================================================================== -->
	<!-- 결제 목록 조회 -->
	<resultMap id="payResultMap" type="com.itwillbs.project.admin.dto.PayDTO">
		<id property="id" column="pay_id" />
		<result property="userId" column="user_id" />
		<result property="userType" column="user_type" />
		<result property="productId" column="product_id" />
		<result property="productName" column="product_name" />
		<result property="payMethod" column="pay_method" />
		<result property="cardName" column="card_name" />
		<result property="cardNum" column="card_num" />
		<result property="bankName" column="bank_name" />
		<result property="depositAccount" column="deposit_account" />
		<result property="depositName" column="deposit_name" />
		<result property="payPrice" column="pay_price" />
		<result property="payDate" column="pay_date" />
		<result property="payStatus" column="pay_status" />
	</resultMap>
	
	<select id="selectPayList" resultMap="payResultMap">
		select 
			p.pay_id
			, p.user_id
			, u.user_type
			, p.product_id
			, pd.product_name
			, p.pay_method
			, p.card_name
			, p.bank_name
			, p.deposit_account
			, p.deposit_name
			, p.pay_price
			, p.pay_date
			, p.pay_status
		from 
			pay p join product pd
		on 
			p.product_id = pd.product_id
		join
			user u
		on p.user_id = u.user_id
	</select>
	
	
	
	
	
	
	
	
	
	
	
	
	
	
</mapper>